一、下载Git
二、在Github新建仓库
复制仓库地址，注意不要初始化选择README.md,否则之后推送到远程分支时会出错
三、本地操作
（1）右键项目，选择Git Bash Here
（2）把Github上的项目复制到本地，git clone 仓库地址，此时本地项目文件夹下会出现一个新的文件夹（此文件夹名为github上的项目名）
[此步骤也可通过自己新建文件夹，然后在cmd下进入此文件夹，然后通过使用git init命令实现]
（3）把本地项目文件夹下所有文件都复制到新文件夹下
（4）cd 新文件夹名
（5）git add .
把新文件夹下面的文件都添加进来
（6）git commit -m "提交信息" ,提交信息可为first commit
[此步骤常出错，如所提交的文件未修改，或本地仓库未更新到最新，则也会出错]
（7）git push -u origin master
此操作把本地仓库push到github上，需要输入github的用户名和密码
[首次push，要加-u， 之后则不需要]
